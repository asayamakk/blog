{
  "name": "blog",
  "private": true,
  "scripts": {
    "build": "./node_modules/.bin/gridsome build",
    "develop": "./node_modules/.bin/gridsome develop",
    "explore": "./node_modules/.bin/gridsome explore",
    "deploy": "rm -rf ./dist && npm run build && ./node_modules/.bin/firebase deploy --token=$FIREBASE_TOKEN"
  },
  "dependencies": {
    "@gridsome/plugin-google-analytics": "^0.1.0",
    "@gridsome/remark-prismjs": "^0.2.0",
    "@gridsome/source-filesystem": "^0.6.0",
    "@gridsome/transformer-remark": "^0.3.0",
    "gridsome": "^0.7.0"
  },
  "devDependencies": {
    "@gridsome/cli": "^0.3.4",
    "firebase-tools": "^9.1.0",
    "node-sass": "^4.12.0",
    "sass-loader": "^8.0.0"
  }
}
